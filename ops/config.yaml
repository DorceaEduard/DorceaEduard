# Ars Contexta Vault Configuration
vault_name: ProiectEdi
created: 2026-06-30
platform: opencode
automation: convention

dimensions:
  granularity: atomic
  organization: flat
  linking: explicit
  processing: heavy
  navigation: 2-tier
  maintenance: condition-based
  schema: moderate
  automation: convention

self_space: false

vocabulary:
  notes_folder: cursuri
  moc_prefix: "MOC"
  note_types:
    - concept
    - definitie
    - teorema
    - exemplu
    - observatie

schema_fields:
  required:
    - description
    - source
    - course
    - type
  optional:
    - created
    - updated

pipeline:
  stages:
    - reduce
    - reflect
    - reweave
    - verify
